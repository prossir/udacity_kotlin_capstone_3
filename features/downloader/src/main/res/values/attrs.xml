<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="DownloadButtonView">
        <!-- Idle states -->
        <attr name="buttonColorIdle" format="color" />
        <attr name="textColorIdle" format="color" />
        <attr name="buttonTextIdle" format="string" />

        <!-- Downloading states -->
        <attr name="buttonColorDownloading" format="color" />
        <attr name="textColorDownloading" format="color" />
        <attr name="buttonTextDownloading" format="string" />
    </declare-styleable>
</resources>